---
import NavMenuList from "./NavMenuList.astro";
const { items = [] } = Astro.props;
---
<nav>
  <!-- Render the top-level menu as a horizontal flex list -->
  <NavMenuList items={items} class="flex gap-4" />
</nav>
