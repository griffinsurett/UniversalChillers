--- 
/*
  This component renders a single menu item.
  It expects an "item" prop with:
    - slug: the link URL
    - label: the text to display
    - children: (optional) an array of submenu items
  It also accepts a "depth" prop (number) to indicate the nesting level.
*/
const { item, depth = 0 } = Astro.props;
import SubMenu from './SubMenu.astro';
---
<li class="relative hamburger-menu-item hover:bg-secondary">
  <a href={item.slug} class="flex items-center text-[var(--color-bg)] py-[var(--spacing-md)] px-[var(--spacing-xl)]">
    {item.label}
    {item.children && item.children.length > 0 && (
      <span class="arrow ml-[var(--spacing-sm)] font-xs transition-transform duration-200">▼</span>
    )}
  </a>
  {item.children && item.children.length > 0 && (
    <SubMenu items={item.children} depth={depth + 1} />
  )}
</li>
